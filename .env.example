# Admin Password (REQUIRED - app will not start without this)
ADMIN_PASSWORD=

# Development/lan (HTTP only, no SSL)
DOMAIN=localhost

# Production
# DOMAIN=research.keabase.dev
# ACME_EMAIL=your@email.com  # optional, for expiry alerts

# SSL Certificate
# USE_OWN_CERT=false (default) - uses Let's Encrypt automatic SSL
# USE_OWN_CERT=true - uses your own certificate from nginx/ssl/:
#   - nginx/ssl/fullchain.crt  (your certificate chain)
#   - nginx/ssl/private.key    (your private key)

# AI Provider API Keys (server-side)
# NOTE: These keys are used for INITIAL DATABASE SEEDING only (first run).
# After first run, all provider settings are stored in SQLite database.
# To add new providers or update API keys, use the Admin Panel at /admin
ANTHROPIC_API_KEY=sk-ant-api03-x
OPENAI_API_KEY=sk-proj-x
GOOGLE_API_KEY=x
MISTRAL_API_KEY=x
XAI_API_KEY=xai-x
OPENROUTER_API_KEY=sk-or-v1-x